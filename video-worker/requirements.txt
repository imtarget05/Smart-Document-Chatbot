# Dependencies for the SDC video-worker (Python 3.11+).

# Queue access (same Neon/Postgres database as the Spring backend).
psycopg[binary]>=3.1.0

# Object storage: Cloudflare R2 is S3-compatible.
boto3>=1.34.0

# ffmpeg/ffprobe are NOT pip packages — install the binaries (the Dockerfile
# does). In production VIDEO_ENCODER_PROFILE=libx264 (CPU) inside containers;
# videotoolbox/nvenc only on a matching host.

# Tests only.
pytest>=8.0