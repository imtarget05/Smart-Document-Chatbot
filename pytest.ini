[pytest]
testpaths = agent/tests tests
pythonpath = agent .
markers =
    asyncio: mark test to run with asyncio
    integration: marks tests requiring external services (Postgres/Qdrant/LLM); excluded from fast CI
    slow: marks tests taking tens of seconds; excluded from the fast CI job
asyncio_mode = strict
