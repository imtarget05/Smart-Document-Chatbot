-- ============================================
-- V3: Simplify schema for single-file chat mode
-- ============================================

-- Add chunks column to documents (stores JSON array of text chunks)
ALTER TABLE documents ADD COLUMN IF NOT EXISTS chunks TEXT;

-- Note: The following columns are no longer used by the application but are kept
-- for backward compatibility. Hibernate validate mode ignores extra DB columns.
-- - vector_collection_id
-- - status
-- - summary
-- - suggested_questions
-- - concept_map
-- - document_ids (in chat_messages)