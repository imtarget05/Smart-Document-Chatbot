# ============================================
# Smart Document Chatbot - Development Docker Compose
# Only starts infrastructure services (DB, Qdrant)
# Backend & Frontend run locally for hot-reload
#
# Usage: docker compose -f docker/docker-compose.dev.yml up -d
# ============================================

services:
  # --- PostgreSQL Database ---
  postgres:
    image: postgres:15-alpine
    container_name: sdc-postgres-dev
    environment:
      POSTGRES_DB: smart_doc_chatbot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- Qdrant Vector Database ---
  qdrant:
    image: qdrant/qdrant:v1.7.4
    container_name: sdc-qdrant-dev
    environment:
      QDRANT__SERVICE__API_KEY: "qdrant_key_123"
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_dev_data:/qdrant/storage
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:6333/healthz"]
      interval: 10s
      timeout: 5s
      retries: 5

  # --- pgAdmin (Database Management UI) ---
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: sdc-pgadmin-dev
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@smartdoc.local
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - tools

volumes:
  postgres_dev_data:
    driver: local
  qdrant_dev_data:
    driver: local
